import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  root: { position: 'absolute', top: '50%', left: '50%', w: '100%', h: '100%', transform: 'translate(-50%, -50%)' },
});